
export class Review {
    // tslint:disable-next-line:max-line-length
    constructor(public itemId: string, public comment: string, public name: string, public rating?: number, public id?: string) {
    }
}
