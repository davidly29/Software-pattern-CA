export class Item {
    // tslint:disable-next-line:max-line-length
    constructor(public name: string, public price: number, public manufacture: string, public quantity: number, public id?: string) {

    }

}
